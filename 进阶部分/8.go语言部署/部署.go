// 创建时间：2026/6/6 下午10:19
package main

//go build 文件名+后缀   打包为exe文件（Windows下），二进制文件（Linux下）

import "fmt"

func main() {
    fmt.Println("Hello Go")
}

/*
=======================================================================
【Go 工程化 - 编译与部署核心学习笔记】
=======================================================================

一、核心优势：静态链接（“拎包入住”）
1. 传统语言痛点：Java 需要装 JRE，Python 需要装解释器和一堆依赖包（pip install）。环境不一致极易导致线上崩溃。
2. Go 的杀手锏：执行 `go build` 时，Go 会把你的代码和所有底层依赖项全部打包进唯一的一个可执行文件（Windows 下是 .exe，Linux 下是二进制文件）。
3. 零依赖部署：把打包好的文件直接扔到全新的服务器上，不需要安装 Go 环境，直接运行即可！

二、核心技能：交叉编译（Cross-Compilation）
1. 痛点：我们平时在 Windows/Mac 写代码，但线上服务器通常是 Linux。
2. 魔法：Go 原生支持在一种系统上编译出另一种系统能跑的程序。
3. Windows 编译 Linux 程序的命令（在 PowerShell 中执行）：
   $env:GOOS="linux"       // 目标操作系统设为 linux
   $env:GOARCH="amd64"     // 目标 CPU 架构设为 64 位
   go build -o myapp main.go // -o 指定输出文件名。如果不加 .exe 后缀，它就是 Linux 可执行文件。

三、进阶技巧：二进制文件“瘦身”
1. 现象：哪怕是几十行的 HTTP 服务，默认编译出来也有十几 MB，因为里面塞满了用于 Debug（调试）的额外信息。
2. 瘦身命令：
   go build -ldflags="-s -w" 部署.go
   - `-s`：去掉符号表。
   - `-w`：去掉 DWARF 调试信息。
3. 效果：能有效缩小打包后的文件体积（通常减小 20%~30%），适合正式上线发布。

四、真实服务器部署：后台守护运行
1. 问题：在 Linux 终端运行 `./myapp` 后，如果关掉终端窗口（如 Xshell），程序会被强制杀死。
2. 简单方案（nohup）：
   命令：`nohup ./myapp > server.log 2>&1 &`
   作用：让程序在后台静默运行，哪怕终端关了它也不死，并把所有的控制台输出（fmt.Println）全部写入 `server.log` 日志文件。
3. 工业级方案（Systemd 或 Supervisor）：
   作用：由 Linux 操作系统级别来接管你的 Go 程序。如果程序因为 Bug 崩溃，系统会瞬间发现并自动将它重启。
=======================================================================
*/